#include "main.h"
#include <string.h>
/**
 *_strlen -> functoin to get the length of the string
 *@s: a string poiter
 *Return: the length of the string
 */
int _strlen(char *s)
{

	return (strlen(s));
}
