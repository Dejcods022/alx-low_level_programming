#include <main.h>

/**
 *_memset -> this memory set function
 *@s: string
 *@b: a character
 *@n: an integer
 *Retutn: a string
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{

	unsigned int a;

	for (a = 0; a < n; a++)
		s[a] = b;

	return (s);
}
